# Sistema de Reconocimiento Facial

## Instalación

1. Descargar el proyecto con un git clone
2. Crear un entorno virtual usando venv:
   ```bash
   python -m venv venv
   ```
3. Una vez creado el venv, abre una nueva terminal en el proyecto y automáticamente se activará el venv
4. Instalar las dependencias necesarias dentro del venv:
   ```bash
   pip install opencv-python opencv-contrib-python
   ```

## Ejecución

Ejecutar el archivo main:
```bash
python main.py
```
