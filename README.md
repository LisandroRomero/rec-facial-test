Descargar el proyetor con un git clone
Crear un entorno virtual usando venv
python -m venv venv
Una vez creado el venv, abre una nueva terminal en el proyecto y automaticamente se te va a ir al venv
Instalar las dependencias que se necesita dentro de venv
pip install opencv-python opencv-contrib-python 
Ejecutar el archivo main
python main.py
